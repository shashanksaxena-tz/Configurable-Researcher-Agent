# Example environment configuration file
# Copy this to .env and configure as needed

# Application Settings
APP_NAME="Configurable Researcher Agent"
VERSION="1.0.0"
API_PREFIX="/api/v1"

# CORS Settings (comma-separated)
CORS_ORIGINS="http://localhost:3000,http://localhost:5173"

# Research Settings
MAX_SEARCH_RESULTS=10
TIMEOUT_SECONDS=30

# Report Settings
REPORTS_DIR="./reports"

# LLM Provider Keys (At least one is required)
# The system prioritizes Google Gemini. If not set or fails, it falls back to OpenAI.
# GOOGLE_API_KEY=your_google_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here
